version: '3.8'

services:
  web:
    build: .
    image: mateonovi/myapp:latest
    container_name: myapp
    ports:
      - "5000:5000"
    restart: unless-stopped
    environment:
      - FLASK_ENV=production